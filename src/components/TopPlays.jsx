import React from "react"
import { Stack } from "@mui/material"

const TopPlays = () => {
  return (
    <Stack flexShrink={0} sx={{ width: { md: "100%", lg: "440px" },  }}>
      TopPlays
    </Stack>
  )
}

export default TopPlays
